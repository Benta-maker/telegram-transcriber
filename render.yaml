services:
  - type: web
    name: telegram-transcriber
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: WHISPER_MODEL
        value: base
    autoDeploy: true
